{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set for the GlareMute desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
